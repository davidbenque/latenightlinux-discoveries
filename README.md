# latenightlinux-discoveries
Extracting the discoveries from the Late Night Linux podcast RSS feed.

## Usage
1. Install the requirements `pip install -r requirements.txt`
2. Run `extract_discoveries.py`
